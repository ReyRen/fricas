
-- Input for page BasicOperatorXmpPage
)clear all

y := operator 'y
deq := D(y x, x, 2) + D(y x, x) + y x = 0
solve(deq, y, x)
nary? y
unary? y
opOne := operator('opOne, 1)
nary? opOne
unary? opOne
arity opOne
name opOne
is?(opOne, 'z2)
is?(opOne, "opOne")
properties y
setProperty(y, "use", "unknown function" :: None )
properties y
property(y, "use") :: None pretend String
deleteProperty!(y, "use")
properties y
