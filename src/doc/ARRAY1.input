
-- Input for page OneDimensionalArrayXmpPage
)clear all

oneDimensionalArray [i^2 for i in 1..10]
a : ARRAY1 INT := new(10,0)
for i in 1..10 repeat a.i := i; a
map!(i +-> i ^ 2,a); a
reverse! a
swap!(a,4,5); a
sort! a
b := a(6..10)
copyInto!(a,b,1)
