
-- Input for page PartialFractionXmpPage
)clear all

partialFraction(1,factorial 10)
f := padicFraction(%)
compactFraction(f)
numberOfFractionalTerms(f)
fractionalTerms(f).3
partialFraction(1,- 13 + 14 * %i)
% :: Fraction Complex Integer
u : FR UP(x, FRAC INT) := reduce(*,[primeFactor(x+i,i) for i in 1..4])
partialFraction(1,u)
padicFraction %
