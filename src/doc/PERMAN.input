
-- Input for page PermanentXmpPage
)clear all

kn n ==
  r : MATRIX INT := new(n,n,1)
  for i in 1..n repeat
      r(i, i) := 0
  r

permanent(kn(5) :: SQMATRIX(5,INT))
[permanent(kn(n) :: SQMATRIX(n,INT)) for n in 1..13]
